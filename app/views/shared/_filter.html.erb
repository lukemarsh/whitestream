<ul class="nav navbar-left navbar-nav">
  <li class="dropdown">
    <%= link_to '#', :class => 'dropdown-toggle', :data => {:toggle => 'dropdown'} do %>
      <b class="caret"></b> <span class="desc">Show all</span>
    <% end %>
    <ul class="dropdown-menu" role="menu">
      <li><%= link_to 'Show all', memes_path, :remote => true %></li>
      <% @meme_categories.each do |c| %>
        <li><%= link_to c.name, memes_path(:filter => c.name), :remote => true %></li>
      <% end %>
      <li><%= link_to 'POPULAR', memes_path(:sort => 'popular'), :remote => true %></li>
    </ul>
  </li>
</ul>